# provider:
#   name: aws
# serviceAccount:
#   annotations:
#     eks.amazonaws.com/role-arn: arn:aws:iam::${ACCOUNT_ID}:role/${EXTERNALDNS_ROLE_NAME:-"external-dns"}

provider:
  name: aws
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<AWS_ACC_ID>:role/<EXTERNALDNS_ROLE_NAME>